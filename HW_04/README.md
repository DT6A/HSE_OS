# Buddy allocator

Файлы `BuddyAllocator.hpp`, `BuddyAllocator.cpp` содержат реализацию.

`main.cpp` содержит некоторое количество тестов.

Собрать можно с помощью ```cmake CMakeLists.txt``` и последующим запуском команды ```make```.

После сборки можно запустить тесты ```./HW_04```.
